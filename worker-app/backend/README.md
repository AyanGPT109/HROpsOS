# HROpsOS Employee Experience — Backend

Backend services for the Worker application.

```
backend/
  services/     # Worker-specific API / Edge Function clients
  contracts/    # DTOs and API contracts
  scripts/      # Ops scripts
```

Configure secrets in environment files for this app only — do not share service-role keys with the frontend.
