import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hrops.superadmin',
  appName: 'HROpsOS Super Admin',
  webDir: 'dist'
};

export default config;
